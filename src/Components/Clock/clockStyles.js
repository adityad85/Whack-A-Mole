import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  time: {
    fontWeight: '500',
    fontSize: 22,
    color: '#000',
    paddingBottom: 5,
    alignSelf: 'center',
  },
});

export default styles;